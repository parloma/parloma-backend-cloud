version: '3'
services:
  server:
    build: .
    links:
      - mongodb
    environment:
      - CONFIG=docker
      - MONGO_URL=mongodb://mongodb
      - MONGO_DB=mongo
      - MAIL_PASSWORD=R0b0tC0nn3ss1
      - MAIL_USERNAME=info@hotblackrobotics.com
    ports:
      - 8080:80
  mongodb:
    image: mongo:3.0.2
